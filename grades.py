import numpy as np

def total_average(grades,weights,default=1.0):
    """
    Args:
        grades (list(float)) : a list of unweighted grades
        weights (list(float)) : a list of weights for each corresponding grade
        default (float,optional) : the default percentage of the grade that will be used to calculate
                                   a missing assignment's value
    Output:
        avg (float) : the average for the course.
    """
    if
